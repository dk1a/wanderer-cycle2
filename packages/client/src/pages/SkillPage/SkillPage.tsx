// import SkillList from "../../components/SkillList";

interface SkillPageProps {
  className?: string;
}

const SkillPage = ({ className }: SkillPageProps) => {
  return <div className={className}>{/*<SkillList />*/}</div>;
};

export default SkillPage;
